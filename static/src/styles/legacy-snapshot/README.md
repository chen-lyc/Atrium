# Auth Styles Snapshot

Snapshot taken before the `auth-entry-redesign-split` branch starts the login/homepage redesign.

Keep this copy as a rollback and visual comparison reference for the current auth homepage CSS. The active auth styles remain in `static/src/styles/auth.css`.
